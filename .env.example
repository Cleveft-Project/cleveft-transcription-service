# Copy to .env. Never commit the real file.
DB_USERNAME=cleveft_user
DB_PASSWORD=cleveft_pass

# Google GenAI key used for speech-to-text and embeddings.
# Without it the service starts, but every upload fails with a clear message.
GOOGLE_API_KEY=
